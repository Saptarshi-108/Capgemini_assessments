3. MovingAverage.java – Moving Average over Last N Values
Problem Statement
Design a moving‑average tracker that:

Keeps the last N numeric values.

When a new value is added and there are already N values, the oldest value is dropped.

Computes the average of the current window on demand.

You must implement 4–5 simple methods and test them in main.
